<?php
$postno = "2015-09-29";
$myposttitle = "Introduction to HTML and CSS";
$mypostauthor = "Marta Pleszynska";
$mypostdate = "Sep 29, 2015";
$mypostcontent = "<p>HTML stands for HyperText Markup Language and used to create web pages. A web browser reads HTML files and render them into visible or audible pages.\n
A simple HTML structure looks like this:
<img class=\"halfsize\"src=\"img/htmlCode.jpg\" alt=\"HTML Code\">
\n
And in a browser:
<img class=\"halfsize\" src=\"img/htmlBrowser.jpg\" alt=\"HTML in Browser\">
<p>Every html page must start with <code><!DOCTYPE html></code>. It defines the version of the document and its type.\n
<code>html</code> tag is the root of the document.\n
<code>head</code> tag is used to contain metadata which are describes in tags like: <code>title style link meta</code>.\n
Title is the title of the document.\n
Style describes the styling.\n
Link contains references to external files.\n
Meta is used to describe the page, include keywords for search engines.\n
<code>body</code> tag describes content visible in a browser.\n
Headers are contain in <code>h1 h2 h3...</code> tags; paragraphs in <code>p</code>; tables in <code>table</code> tag and so on...</p>
<p>The important thing is to remember to close the tag: <code>/html /body /h1</code> etc.\n
There are of course exceptions to the rule: <code>img, input, br, hr, meta</code> tags do not have closing tags.</p>
<p>Tags have properties and attributes. Property in put inside a opening tag after its name and attributes in similar way however the value needs to be specified:\n
<code> \"<'tagname' 'property' 'attribute'=\"value\"></tagname>\"</code></p>
<p>CSS stands for Cascading Style Sheets and it a stysheet language that describes how HTML document should be presented. You can change font size, family or colour, background, apply border and many more.\n
Here a simplified code structure in css:</p>
<img class=\"halfsize\" src=\"img/cssCode.jpg\" alt=\"CSS code\">
<p>This example will change background colour to blue for whole html body.</p>
<p>Browser may have different default styles for html tags so if no style applied html page may look bit different depend on a browser.
There are three ways on applying css to html:\n
<ol><li>In line css - applied in inner tags: <code> tagname style=\"background-color: blue;\"</code></li>
<li>In head tag inside html <style></style> </li>
<li>In separate css file</li>
</ol>
<p>To find out more about HTML and CSS visit following links:\n
<ul>
<li><a href=\"http://www.w3.org/TR/html\">w3 - html</a></li>
<li><a href=\"http://www.w3schools.com/\">w3schools</a></li>
<li><a href=\"http://html.net/\">HTML.net</a></li>";
?>
